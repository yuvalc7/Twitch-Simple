//maintain this history object as opposed to react router becuse it wiil be lot easier to get access it and change whate page the user is looking at
import { createBrowserHistory } from "history";

export default createBrowserHistory();
